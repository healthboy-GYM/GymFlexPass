rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 공개 자산(영상) 폴더
    match /assets/{allPaths=**} {
      allow read: if true; // 누구나 읽기
    }

    // 그 외는 로그인 사용자만(필요 시 조정)
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}